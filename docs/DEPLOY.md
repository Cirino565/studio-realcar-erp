# Deploy / Hospedagem

A documentação principal de hospedagem desta versão está em:

```txt
docs/HOSPEDAGEM.md
docs/RAILWAY_RENDER_VERCEL.md
```

Resumo:

```bash
npm run hosting:check-env
npm run deploy:build
npm run start:host
```

Em produção/hospedagem use PostgreSQL. SQLite é somente para teste local.
